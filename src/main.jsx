import React from 'react';
import ReactDOM from 'react-dom/client';
import ESOPCalculator from './ESOPCalculator.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ESOPCalculator />
  </React.StrictMode>,
);
